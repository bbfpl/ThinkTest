# coding=utf-8
import unittest
from core.core import Test,Tool

class {{ClassName}}(Test,Tool):
	# {{name}}
	{% for item in funs %}
    def {{item.name}}(self):
		# 正常登录
		config_data = self.get_yaml()
	{% if item.data.mode == "post" %}
		data = {{item.data.data}}
		r = self.request('api', type='{{item.data.mode}}', data=data, token=False)
	{% else %}
		r = self.request('api','{{item.data.mode}}')
	{% endif %}
		print(r)
		self.assertEqual(1, 1)
	{% endfor %}

#运行当前py
if __name__ == '__main__':
	unittest.main()