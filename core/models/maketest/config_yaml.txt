#Project项目
Project:
  #account模块
  account:
      #登录 login.py
      login:
        url: /api/login  # 登录
        name: 登录  # 接口注释
        className: TestAccountLogin  # 接口类名
        funName:
          # 接口方法名
          test_account_login: {
            # 请求类型
            mode: post,
            # 参数
            data: {
              loginName: admin,
              password: 000000
            }
          }